## 知识库网站结构调整计划

### 1. 目录结构调整
- **前端文档整合**：
  - 将 `React`、`Typescript`、`Electron`、`DesignPatterns` 文件夹移动到新创建的 `src/Notes/Frontend` 文件夹下

- **后端文档组织**：
  - 在 `src/Notes` 下创建 `Backend` 文件夹
  - 将现有的 `Java` 文件夹移动到 `src/Notes/Backend` 文件夹下

### 2. 配置文件修改（.vitepress/config.mjs）
- **头部导航菜单**：
  - 将"学习笔记"子菜单改为"前端"和"后端"
  - 前端菜单链接到 `/Notes/Frontend/React/Start`
  - 后端菜单链接到 `/Notes/Backend/Java`

- **侧边栏配置**：
  - 调整为四个侧边栏：前端、后端、面试题、代码片段
  - 前端侧边栏包含 `Frontend` 下的所有文档
  - 后端侧边栏包含 `Backend` 下的所有文档
  - 面试题和代码片段保持原有结构

### 3. 首页修改（src/index.md）
- **Hero 区域按钮**：
  - 改为"前端"、"后端"、"面试题"
  - 分别链接到 `/Notes/Frontend/React/Start`、`/Notes/Backend/Java`、`/Interview/HTTP`

### 4. 首页组件修改（src/components/Home/navList.js）
- 更新导航卡片数据：
  - 改为前端、后端、面试题、代码片段
  - 分别链接到对应页面

### 实施步骤
1. 首先创建必要的目录结构
2. 移动文档文件到新目录
3. 修改配置文件中的导航和侧边栏
4. 更新首页和组件内容
5. 测试导航链接是否正常工作

### 预期效果
- 清晰的前后端分离结构
- 更直观的导航体验
- 符合用户需求的菜单和按钮布局